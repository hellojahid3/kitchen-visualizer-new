/**
 * Prettier configuration file with Tailwind CSS plugin.
 *
 * @see https://prettier.io/docs/en/configuration.html
 * @type {import('prettier').Config}
 */
const config = {
  semi: true,
  tabWidth: 2,
  printWidth: 100,
  singleQuote: true,
  arrowParens: 'avoid',
  trailingComma: 'es5',
  singleAttributePerLine: true,
  endOfLine: 'auto',
};

export default config;
