/**
 * Viewport configuration for the layout.
 */
export const VIEWPORT_CONFIG = {
  minScale: 0.9,
  maxScale: 3,
  zoomStep: 0.1,
  extensionLimit: 10,
  fullscreenExtension: 100,
  aspectRatio: 16 / 9,
};
